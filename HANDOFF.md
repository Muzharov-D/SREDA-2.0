# Среда — HANDOFF для новой сессии

> Прочитай этот файл первым. Он задаёт **заземлённое** направление. НЕ возвращайся к «космосу» (см. ниже).

## 🟢🟢🟢 Сессия 2026-06-11 (вечер) — БЭКЕНД ПОСТРОЕН + ПОРТАЛ v2 + АУДИТ (САМОЕ СВЕЖЕЕ, ЧИТАТЬ ПЕРВЫМ)
Развилка «демо vs бэкенд» РЕШЕНА в пользу бэкенда. Всё на проде. Коммиты dbe8133…9d1fc9e.

**Бэкенд (`server/app.js`, Express 4 + sqlite3):** файл `server/data.db` (авто-сид при пустой БД через `server/seed.js`), `npm start`=`node server/app.js`, PORT=env||3000. Auth: header `X-API-Key`===`sreda-prototype-key-2026` (тот же ключ захардкожен в `assets/mock.js`, `/api/health` без ключа). CORS открыт. Отдаёт статику всего репо + `/portal`→portal.html, `/`→index.html. Роуты: CRUD `/api/agents|projects|bills|audit`, **`/api/messages`** (чат: GET `?channel=tg:<id>` тред, POST шлёт сообщение + симулирует ответ агента: задача→«принял»→через 6-10с артефакт; `/messages/:id/accept`→счёт, `/messages/:id/return`→доработка), `/api/tasks`, PATCH оплаты счёта, **POST `/api/demo/reset`** (сброс демо-данных). **⚠️ Корневой `server.js` УДАЛЁН** — это был старый статический сервер на 3000, его стейл-процесс отдавал index.html вместо API и был ГЛАВНОЙ причиной «портал не работает». `render.yaml`=web service (`node server/app.js`), не static.

**Портал v2 (`/portal`):** дефолтный экран **«Сотрудники»** — общение с нанятыми ЦС через привычные каналы (как просил заказчик). Новые `assets/portal-chat.js` + `assets/portal.css`. Слева контакты-сотрудники (статус в сети/на задаче, бейдж непрочитанных), справа тред с вкладками **Чат** (Telegram-стиль: пузыри, «печатает…», постановка задач в чате, артефакты-вложения с Принять/Вернуть) и **Почта** (отчёты, счета письмами) + мок «Подключить в свой Telegram/Email». Найм из каталога → редирект в чат с приветствием. **Без бэкенда живёт на локальной симуляции** (статика Vercel ок). Меню портала: Сотрудники·Каталог·Заказы·Счета·Поддержка. Починены баги прошлой полусборки: исчезающий счёт после найма, двойной биллинг, портал не читал API, нет персиста/portal-mode.

**Навигация Inside:** единый `navTo` для ВСЕХ кликов (было 3 рассинхронных пути), back-stack `goBack` с подписью «← Назад · откуда» и восстановлением скролла, хлебные крошки `.nav-crumbs` **ВНУТРИ `.work`** (вставка рядом со `#stage` ломает grid — уже наступали, HOTFIX 85eefd3), анимация входа `stageIn`.

**Пульс-карты — бинарный код + профиль каждому узлу:** люди = синий круг `#60a5fa`, цифровые = изумрудный ромб `#36c994` (правки в `cortex.js`/`neural.js`/`app.js`; цвета отделов остались в подписях/связях, не в людях). Каждый безымянный «добивочный» узел теперь получает **детерминированный профиль** — новый `assets/people-gen.js` (`genPerson`/`genWorker`, хеш от dept+kind+index, реестр `window.__PEOPLEGEN`), ховер показывает имя+роль, клик → роуты `gperson:<dept>:<i>` / `gworker:<dept>:<i>` (рендер в app.js, родная вёрстка профиля). Обе карты: числа сходятся со штатом (dev 52 чел / 56 цифр).

**Дизайн-системный аудит (skill design-system) → фиксы (коммит 47eae6f):** аудит дал 52/100. Применено в `styles.css`: удалён дубль `.btn` (перебивал канон по каскаду), токен `--on-acc:#06231b` для текста на акценте, единый фокус `var(--acc)`+кольцо (заменено ~114× старого зелёного `rgba(52,211,153)`→`rgba(54,201,148)`), `color-scheme:dark`, глобальный `prefers-reduced-motion`, `.kpi:hover`, color-mix фолбэки, 5 чат-композеров сведены визуально, токены-документация `--fs-*`/`--sp-*`. В `app.js`: разгружена карточка каталога найма (скиллы→в профиль `tagent:`), a11y текст-альтернатива для canvas-карт. Портал: reduced-motion для стрим-печати, тост при ошибке отправки, скелетоны. ⚠️ Правка «воздуха в кокпитах» легла в `renderCockpit` — он МЁРТВАЯ ветка (реальные кокпиты идут через `ENVS2`/`renderEnv`; `pulse` перехватывает раньше `organism`). Настоящие кокпиты не разгружены — отдельная задача.

**⚠️ РЕГРЕССИЯ И ФИКС (коммит 9d1fc9e):** после аудита экраны глубже пульса компании стали ПУСТЫМИ — `.work` залипал в `opacity:0`, потому что `stageIn` шёл с `fill-mode:both` и не доигрывал. Исправлено: **`stageIn` больше не трогает opacity вообще** (анимирует только сдвиг), `.work` всегда `opacity:1` — пустого экрана быть не может. **ВАЖНО ПРО КЭШ:** на `/assets/*` стоит `Cache-Control: max-age=3600` — после правок CSS/JS нужен ЖЁСТКИЙ рефреш (Ctrl+Shift+R), иначе браузер держит старый файл до часа (именно кэш усугубил видимость этой регрессии).

**Деплой — текущее состояние:** фронт **Vercel** (`sreda-2-0.vercel.app`, автодеплой из main), бэк **Render** (`sreda-2-0.onrender.com`, web service). `assets/mock.js` API_BASE: localhost→`:3000`, иначе same-origin. ⚠️ Пользователь УБРАЛ rewrites из `vercel.json` (теперь только `cleanUrls`/`trailingSlash`) — значит **прокси `/api/*` Vercel→Render ВЫКЛЮЧЕН**, на Vercel портал работает в локально-симуляционном режиме (без живого бэка); живой API только на самом Render-домене. Если нужна одна ссылка с живым API — вернуть rewrite в vercel.json (см. коммит f86cb5a). Render Build Filter (Included Paths `server/**`, `package.json`) настроен пользователем в дашборде — пуши фронта не пересобирают бэк/не сбрасывают БД (диск Render эфемерный).

**Параллельная работа пользователя (НЕ мои коммиты, не задокументировано):** «Singular Metropolis» — альтернативная тема/концепт-страница платформы (коммиты 7246183, 8495923, 0eeffd0). Детали в соответствующих файлах — посмотреть при необходимости. `office.html` — untracked single-file canvas-визуализация офиса разработки (top-down, аватары `assets/avatars/dev-*.jpg`), не связан с порталом/API.

**Стоимость сессии ~$138** (пользователь просит не останавливаться, но озвучивать цифру — см. память `never-stop-for-budget`). Тяжёлые задачи — ТОЛЬКО субагентам/workflow, основной контекст беречь.

## 🔥 Сессия 2026-06-11 (продолжение) — развилка «демо vs бэкенд» (РЕШЕНА выше, оставлено для истории)
Сделано после блока «ТРИ ФАЗЫ» ниже (коммиты 9e98f66…e583766, всё на проде):
- **Мегапроект CEO живой**: Пульс → кнопка «🚀 Проекты компании» = режим проекта (фокус: 17 исполнителей + золотые кольца, фоновая жизнь гаснет, трасса бежит и закрывает active-задачи). Статусы живые (`projTaskState`): blocked Иры = реальный sev1-гейт юр-стола (в панели кнопка «снять риск → рабочий стол»; снял руками — задача «в работе», фаза «Запуск» открывается, на 100% — финал-волна «Среда запущена»). **Гибридные пары**: у каждой задачи цифровой напарник `projDW()` (Ира + 🤖 Статут) — подсвечен на карте, несёт черновик на трассе, виден в панели и чипах.
- **«Запустить рой» = пусковая на 3 уровнях**: Пульс компании → «Пусковая Среды» (очередь `PROJECT_QUEUE`: ОАЭ, отток −20%; запуск → проект «в полёте», мультипроектная панель/трасса); пульс отдела → очередь бэклога доски (запуск реально двигает канбан backlog→work + назначает исполнителя); задачи — ⌘K.
- **Онтология зафиксирована в UI**: Умение=атом · Рой=связка при человеке (отвечает носитель) · Цифровой сотрудник=субъект (ДИ/KPI). Библиотека: 3-я полка «Рои под должность» (из ROLE_SWARM, глагол «Подключить»), глоссарий-строка; «Командные рои · не штат» в команде; RPG: рой=«Фамильяры».
- **Лестница «С чего начать» (modules)**: коммерческая = Проект на аутсорс → Фабрика → Inside; умение/рой/ЦС — блок «из чего сделано», не ступени продаж.
- **Внешний контур**: воркспейс «Платформа Среды» (клиент без Inside) + **`/portal.html` — отдельный продукт** (свой URL/бренд/OG, 4 пункта меню, найм в каналы клиента, апселл Inside, ₽-метр → счета). `isExtWS()`/`window.__PORTAL`/`body.portal-mode`.
- **Персистентность**: `sredaPersist/sredaRestore` (talent.js) — сторы `__TALENT/__SREDA_INV/__FORGE/__PROJST` в localStorage `sreda_state`, переживают F5; «Сбросить демо» в Счетах. Известный пробел: кристаллизованные в DIGITAL_STAFF не персистятся.
- **Ревью пользователя (gap-анализ)** принято; РАЗВИЛКА не решена: (а) демо-полировка — остатки P0: статусы людей (active/перегружен/отпуск), PEOPLE_DETAIL 95→150, density-вид нейрокарт; (б) **бэкенд** — старт от tech-spec.agent.final.md: Node/Express+Postgres, первые контракты = 4 localStorage-стора (`/agents /contracts /projects /invoices`), фронт переключается со сторов на fetch без переписывания визуала. Пути НЕ параллелить. Пользователь склоняется к бэкенду («если нужно писать бек — давай писать бэк»), но решение за ним.
- Правила: пуш в main = прод (Vercel); cost-警告 игнорировать по инструкции пользователя, но озвучивать цифру; превью: rAF в фоне мёртв (шим `requestAnimationFrame=cb=>setTimeout(...,30)`), хук `__CX`.

## ⭐⭐⭐⭐⭐ Сессия 2026-06-11 — ТРИ ФАЗЫ: Talent + Forge поверх Inside
Реализованы фазы из pitch-deck.agent.final.md (фазовая модель: Inside=твёрдое, Talent=жидкость, Forge=газ). Смотрим глазами CEO Кирилла: нав-группа «Платформа Среды» в Менеджменте (Цифровой найм · Цифровое производство · Счета Среды). Новые файлы **assets/talent.js**, **assets/forge.js** (подключены до app.js).
- **Talent** (`renderTalent`/`renderTalentAgent`, роуты `talent`, `tagent:<id>`): пул 154 агента (16 витринных вручную + детерминированный хвост `talentCatalog()`); имена = человеческое имя + «цифровая» фамилия (Вектор, Квант…), **hex-аватар** (люди=круг, Inside-цифровые=ромб, Talent=шестиугольник); карточка: рейтинг/приёмка/портфолио/отзывы/домены, сравнение с человеком **в сроках и часах** (не в зарплате!), **паспорт агента** (timeline фаз, `tlPassportHTML`, только обезличенные навыки чужих компаний); **живое собеседование** (TL_TESTS: уточняющий вопрос агента → решение стримом → вердикт-оценки → оффер); найм staffing/result с чек-листом «4 минуты» → агент появляется в команде отдела (`renderTeam`, секция «Агенты Среды на контракте», sel.kind==='t'), на нейрокарте отдела (узел .tlq, бирюзовый hex, связь с лидом) и в контракт-карточке (периметр 2/6, ставка, принятые результаты); постановка задачи → приёмка → инвойс; **кристаллизация** `tlCrystallize` → push в DIGITAL_STAFF/ALL_DIGITAL («экс-Talent», полная ДИ).
- **Forge** (`renderForge`/`renderForgeProject`, роуты `forge`, `fproj:<id>`): каталог 8 сценариев (цены из дека, без сроков), сложность ×0.7/1/1.5 с живым пересчётом, заказ → `fgOrder` собирает бригаду из пула Talent по ролям; **производственный конвейер** (.fg-line: станции с заливкой-прогрессом, бегущая лента, шлагбаумы-ворота, деталь едет, искры на активной станции); **живые ворота** (утвердить → этап-инвойс 30/40/30; вернуть — комментарий обязателен → видимая переработка −45%); таймер автопаузы; «лента цеха» — анонимизированные события (`FG_FEED`, изоляция=безопасность); критерии приёмки закрываются замерами в тестировании; 3 преднаполненных проекта: **fp-neuro — мета-флагман «Модуль „Нейрокарта отдела" для Среды» (сдан; артефакт «Открыть результат в продукте» → dpulse:dev!)**, fp-demand (ждёт ворот-1), fp-clinic (в работе); после сдачи — «нанять бригаду в Talent» (лестница доверия).
- **Счета Среды** (`renderSredaBills`, роут `bills`): инвойсы Forge-этапов и Talent, KPI «эквивалент в часах людей»; гибрид: `updateMeter()` — ₽-метр шапки = 184k + sredaSpend(), обновляется при каждом pushInvoice.
- Пасхалка работает и в talent-вводе. Проверено: 106/106 маршрутов, 0 ошибок; найм/ворота/возврат/кристаллизация/заказ из UI — прогнаны.
- ⚠️ Незакрытые хвосты: DIGITAL_HEADCOUNT не растёт при кристаллизации (счётчики штата +1 не учитывают); собеседование в фоновом превью стримит медленно (троттлинг — в живом браузере ок).

## ⭐⭐⭐⭐ Сессия 2026-06-10 (ночь, продолжение) — КОРТЕКС: вся компания нейронами (самое свежее)
Фидбек пользователя на макро-пульс: «думал, что-то более революционное, это халява» → флагман переписан с DOM-коробок на **canvas-кортекс**. Новый файл **`assets/cortex.js`** (`cortexMap`), подключён после neural.js.
- **Все 314 сотрудников — нейроны на одном экране**: 8 кластеров-отделов (филлотаксис вокруг центра кластера), именные люди = крупные круги цвета отдела, типовые = мелкие, цифровые = ромбы-изумруд; числа сходятся со штаткой (именные + добивка до HEADCOUNT/DIGITAL_HEADCOUNT).
- **Синапсы между конкретными людьми** (DEPT_SYN, кривые в обход ядра, толщина=объём), импульсы с подписями на canvas (roundRect-плашки), **сюжетные цепочки FLOWS** (`runStory`: Оля→Ира→Аня по людям, артефакты подписаны, гейт sev1 = красный застрявший импульс + «⛔» в ленте), **волна-дыхание** от ядра (`surgeWave`), гейт-кольцо вокруг кластера с открытым sev1.
- **Интерактив**: ховер нейрона = тултип (имя·роль·сейчас / «Специалист отдела»), клик = профиль (person:/worker:/team:), DOM-чипы кластеров → dpulse; **зум колесом** (1–2.6×, пан перетаскиванием), докрутил до упора над кластером → автопогружение в его пульс. Ядро-лого масштабируется через `--zoom`.
- `window.__CX` — отладочный хук (e2e). destroy() флашит импульсы (resolve false) и снимает листенеры; гибель экрана ловится и в frame(), и в интервале.
- ⚠️ **rAF в фоновой вкладке превью НЕ тикает вообще** (canvas пустой) + setTimeout клампится до 1с — проверять через шим `window.requestAnimationFrame=cb=>setTimeout(()=>cb(performance.now()),30)` и хук __CX; в реальном браузере всё живое. Проверено: 99/99 маршрутов, 0 ошибок, тултипы/клик/зум-дайв/импульсы — через __CX.
- Старые `.pls-dep`/`.pls-link`-стили остались в CSS (мёртвые для pulse, не мешают).

## ⭐⭐⭐ Сессия 2026-06-10 (ночь) — RPG-СЛОЙ + НЕЙРОННЫЕ КАРТЫ
Реализован `concept-redesign.md` целиком (одобрено пользователем). Новые файлы: **`assets/rpg.js`** (данные+лексикон), **`assets/neural.js`** (движок карты). Подключены в index.html ПЕРЕД app.js.
- **95 именных людей всех 8 отделов проработаны вручную** (`PEOPLE_DETAIL`, ключ `отдел:Имя`): `lvl` 1–5, `sk` 4 компетенции с оценками, `mi/du/li/esc` (мини-ДИ как у цифровых), `ach` 3 достижения с цифрами. Фолбэк `personRPG()` — детерминированный из ROLE_SWARM. Квесты `questsOf()` — из досок COCKPITS[*].board (who===имя) + текущий task; `gate:true` у merge-карточек → красный «⛔ sev1-гейт» когда stepGate(role).gated.
- **Hero Card** в `renderTeam` (правая панель, `.hc-*`): загрузка-бар, компетенции-точки, задачи с прогрессом, рой, достижения. Цифровой → Creature Card (`.crd`): мана-бар (KPI задач/нед), характеристики из модели (`MODEL_STATS`: haiku=скорость, opus=качество), умения со звёздами, ограничения с ⛔, активная задача с прогрессом и артефактами.
- **Профили**: у человека новая вкладка `rpg` («Компетенции») — скиллы+квесты+ДИ человека+достижения; бейдж уровня в шапке. У цифрового новая дефолтная вкладка `stats` («Характеристики»): мана/статы/умения/ограничения + активная задача.
- **Нейронная карта микро** — `renderDeptPulse` переписан: нейроны = именные люди (аватар, размер=загрузка, точка-статус) + цифровые (ромбы) + кластеры-функции (числа сходятся со штатом через allocByWeights). Синапсы: лид↔функции, цепочки внутри функций, цифровой→его руководитель. Импульсы подписаны исполнителем, при приходе нейрон «возбуждается» (.fire) + всплывашка; **тормозной синапс**: при гейте каждые ~7с красный импульс застревает на 54% и мигает (.stuck), нейрон лида st-block. Ховер по нейрону подсвечивает его синапсы. Клик: человек→person:, цифровой→worker:, кластер→team:.
- **Макро** — `renderPulse`: 12 межотделовых синапсов `DEPT_SYN` (rpg.js), толщина=объём передач/нед, передачи летят по ним взвешенно с реальными артефактами («КП «Гамма» → договор»).
- **Пасхалка «Шевелись, плотва»**: фраза в ЛЮБОМ вводе (глобальная модалка ⌘K, ассистент, постановка задачи цифровому ×3 места) → `rpgPhraseCheck()` → toggle RPG-лексикона (`LEX()`: Загрузка→Здоровье, Умения→Заклинания, Границы→Проклятия, Рой→Призванные, Грейд→Lvl, кнопка → «⚔ Открыть свиток героя»), body.rpg-mode = золотые акценты #e8c468, persist в localStorage `sreda_rpg`, повторный ввод выключает. В RPG-режиме на нейрокарте редкие импульсы «🐴 Плотва · срочный квест».
- **Движок neural.js**: `neuralMap(stage,{nodes,links,layout,onClick})` → impulse/fire/destroy; `neuralClusterLayout` — кластеры на эллипсе, участники вокруг. ResizeObserver + rAF + setTimeout(160) против нулевой ширины при первом рендере.
- **Верифицировано**: 141/141 экранов рендерятся, 0 ошибок консоли; пасхалка туда-обратно; гейт-импульс застревает; макро-артефакты летят. ⚠ Превью-вьюпорт может схлопываться до 33px — это превью, делать `preview_resize` 1600×950 и reload, НЕ чинить вёрстку.

## ⭐⭐ Сессия 2026-06-10 (вечер) — ПИТЧ-ДЕК, ПАЙПЛАЙН, GAPS
Пользователь принёс 5 документов-гипотез (бриф v5 «Цифровой EPAM», архитектура КОРУС v1.0 на базе **рабочего продукта Sreda v0.8.87**, обзор+пустой PPTX-шаблон для руководителей, HTML-архитектура v2). Сделано:
- **`/deck.html`** — питч-дек, 15 слайдов в бренде (стрелки/PgDn), на проде: sreda-2-0.vercel.app/deck.html
- **`docs/PIPELINE.md`** — пайплайн: 3 актива (ядро v0.8.87 + наш прототип как дизайн-спека Cowork-UI + LLMaaS Впрод) → фазы 0–3, маппинг экранов прототипа на модули, команда, гейты
- **`docs/GAPS.md`** — 20 упущений; топ: два бренда (Авандок vs КОРУС), data flywheel vs изоляция тенантов, эскалация человеком, юридика «оплаты за результат», коннекторы 1С/ЭДО, гео-обход = юр. риск
- Сведённое позиционирование: «цифровые сотрудники, которым можно доверять» сейчас; «цифровой EPAM» — видение этапа 3.
Открытые решения за пользователем: бренд, якорный пилот-заказчик, владелец продукта.
Прод: https://sreda-2-0.vercel.app (Vercel автодеплой из main). Репо: github.com/Muzharov-D/SREDA-2.0.

## ⭐ Сессия 2026-06-10 — «Авандок Среда 2.0»: ребрендинг + цифровой штат (АКТУАЛЬНОЕ СОСТОЯНИЕ)
Полная переработка под показ «завтра отдаём в работу». Решения согласованы с пользователем:
- **Брендинг под логотип SREDA**: тёмный тёплый графит (#121310…#1f221d), изумрудный акцент `--acc:#36c994`, премиальные тени/свечения (`--glow-acc`). Светлая EDP-тема удалена (токены в `:root` переписаны). Новый `logo.svg` — гексаэдр-куб с пульсирующим изумрудным ядром (SMIL-анимация). Wordmark в шапке: **«АВАНДОК СРЕДА 2.0 · Больше чем результат»** (`.brand-t`).
- **Пульс компании** (`renderPulse`, `.pls-*`) — флагман, экран по умолчанию, в меню CEO: **лого в центре** (PULSE_LOGO, вращающиеся орбиты, дыхание ядра), вокруг — 8 реальных отделов (👤люди + 🤖цифровые), частицы-задачи летят по кривым (offset-path), передачи между отделами, живые счётчики/лента, «⚡ Запустить рой» (surge + аудит), клик по отделу → его пульс. Старый DOMAINS-хаб удалён из renderPulse.
- **Цифровой штат ≥50%**: `DIGITAL_HEADCOUNT` (164), `TOTAL_STAFF=314`, `DIGITAL_SHARE=52%`. **`DIGITAL_STAFF`** в mock — 54 именных цифровых сотрудника по отделам, у каждого: должность, функция (= fn людей), руководитель-человек, модель, статус (active/probation/paused), `ji` = **должностная инструкция** (миссия · обязанности · границы-запреты · KPI · эскалация). `ALL_DIGITAL` — плоский список.
  - **Команда отдела** (`renderTeam`): цифровые в одних функциональных группах с людьми (бейдж «цифровой»), клик → панель ДИ, прямо из панели можно **поставить задачу** (реагирует: now-строка, тост, аудит). Заголовок: «N людей + M цифровых (K%)».
  - **Оргструктура** (`renderCompany`): штат 314, стек-бары люди/цифровые, цифровые в дереве функций, клик → профиль.
  - **Штат цифровых сотрудников** (`renderWorkerWorkforce`, owner): реестр 54, поиск/фильтры, клик → **профиль** (`renderWorkerProfile`, ALL_DIGITAL): полная ДИ + управление (поставить задачу, пауза/возобновить, отчёт руководителю — всё пишет в аудит).
- **CEO переосмыслен** — бизнес-кокпит: меню = Пульс · Дашборд · Оргструктура · Передачи компании · Проекты. Дашборд переписан: бизнес-KPI (штат, доля цифровых, потоки), синтез недели, загрузка 8 отделов (клик → отдел), след «Гаммы», передачи (кликабельны), очередь решений (одобрение → аудит). Федерация/контракты/арбитраж/экономика **убраны из nav** (код жив, экраны доступны только по прямым роутам).
- **Владелец платформы переосмыслен** — управление Средой: Штат цифровых · **Бюджеты ИИ** (новый `renderAIBudgets`: редактируемые лимиты по отделам — onchange реагирует, живые политики-переключатели, топ-5 потребителей) · Маршрутизатор · Аудит · Библиотека · Студия; раздел **«Демо · Вижн»** (nav-sep): Суперсила, Путь, Контур, Battle.
- **Ввод реагирует везде**: глобальная модалка «Поставить задачу Среде» → роутинг по ключевым словам → ассистент роли с **автоотправкой** (`window.__PENDING_ASK`); канал отдела — живой композер (цифровой сотрудник отвечает, аудит); ₽-метр в шапке кликабелен → Бюджеты ИИ; каналы досеяны для всех 8 отделов.
- **Починены критические баги прошлой сессии**: частичная замена «skills→умения» ломала ассистентов всех ролей и библиотеку (`inst.умения`, `LIB.умения`/`LIB.skills`, `dep.умения`, `m.умения`, `#agУмения`) — всё нормализовано на `skills`. Удалены мёртвые дубли (~530 строк): старые renderWorkerWorkforce/renderWorkerProfile/renderDeptChannel/deptColor/legacy.
- `pushAudit` нормализован (`act`/`emoji`/`dept` по умолчанию). `init()` уважает `location.hash`. Тур обновлён: старт с Пульса, финал — цифровой штат.
- **Верифицировано**: 74/74 экранов рендерятся, 0 JS-ошибок, консоль чистая (`preview_eval`; скриншоты превью по-прежнему виснут — не использовать). Бэкап до правок: `_backup/2026-06-10/`.
- **Золотые профили (суть EDP, не визуал)**: слоган исправлен на **«Больше, чем результат»** (запятая!). `renderPersonProfile` (роут `person:<dept>:<idx>`) — полноценный профиль человека: шапка-карточка (инициалы-аватар в цвете отдела, ФИО — фамилии генерятся детерминированно `personOf()`/`strHash`, грейд, табельный, «Индекс Среды» ★), чипы систем-источников (1С ЗУП/КЭДО/CRM/ATS/K-TEAM/Среда), **у каждого поля бейдж источника** (`gpField`/`SRC`), 7 вкладок: Основное · Трудовое · Рой и ИИ (постановка задачи рою — живая) · KPI · Доступы · Активность · Документы. Входы: кнопка в штате отдела, клик по человеку в оргструктуре и в канале. `renderWorkerProfile` переделан в тот же формат: 6 вкладок (ДИ · KPI·SLA · Доступы/интеграции `DEPT_INTEG` · Бюджет из aiBudgetStore · Журнал · Управление с паузой), кнопка «Профиль руководителя →» связывает цифрового с человеком. Аватары людей в списках штата/канала — инициалы в цвете отдела (`.gp-av`). CSS-блок «ЗОЛОТОЙ ПРОФИЛЬ». Не склонять имена в строках («Аняу» — нельзя): шаблоны «Задача для «X»…». Проверено: 95/95 экранов, 0 ошибок.
- **Равноправие кликов («никакой цифровой дискриминации»)**: чип «профиль →» на каждой строке штата (и люди `data-pjump`, и цифровые `data-wjump`), авторы сообщений в каналах кликабельны (`.ch-au` → person:/worker:, имена цифровых в сидах каналов сведены к DIGITAL_STAFF), у ассистента кнопка «Мой профиль →» (поиск по `personIdxByName`).
- **Деплой**: проект в git (ветка `main`, чистые 15 файлов ~300KiB; артефакты в `.gitignore`). `vercel.json` (статика, кеш ассетов), `render.yaml` (static site blueprint), `README.md` — инструкции запуска/git/деплоя. Vercel: Import → Other → Output `./`. Render: Blueprint из render.yaml.
- **Иконки вместо эмодзи** (запрос пользователя «эмодзи воняют бесплатным продуктом»): `assets/icons.js` — ~70 монохромных line-иконок (24×24, stroke=currentColor, стиль Lucide) + карта EMOJI_ICON (~150 эмодзи) + **глобальный конвертер**: TreeWalker по текстовым нодам + MutationObserver на body — заменяет эмодзи на SVG во всём UI, включая живые ленты и динамические рендеры. Аватары людей → единый user-знак, цифровые → bot (изумруд). Контекстные размеры/цвета — блок «ИКОНКИ» в styles.css (`.ic`, `.ni-ic .ic` и т.д.). Типографские символы (▶ ⏸ ★ → ↔) оставлены намеренно. Новые эмодзи в данных подхватываются автоматически, если есть в карте — иначе добавить в EMOJI_ICON. Плейсхолдеры/атрибуты конвертер не трогает — туда эмодзи не писать.

## TL;DR — что это сейчас
**Среда** — внутренний AI-продукт компании. Один инстанс = одна компания. Не заменяет людей, а **прокачивает**. Главное правило продукта: **«как будто уже внедрено», понятные и контролируемые процессы, без сложных интеграций с внешним миром** (интеграциями займутся спецы отдельно).

Это **кликабельный прототип**: статический HTML + CSS + vanilla JS на моковых данных. Бэкенда нет и пока не нужно.

## Почему развернулись (фидбек питча — обязателен к учёту)
Инвесторам **не зашёл космос** (живой граф компании, само-улучшение, роадмеп до AGI, Battle). Просили:
- UI **для каждого отдела**, и понять **какие скилы и агенты реально пригодятся**.
- Срез **по сениорити**: от стажёра-маркетолога и бухгалтера до фин.директора и владельца.
- **Химию между агентами и реальными людьми** — повседневные, контролируемые процессы.
- Ответ на вопрос: **«как 10 000 личных чатов = Среда?»**

## Архитектура (главный тезис, держать в голове)
**Личный чат — атом. Соединительная ткань — Среда.** 10 000 личных рабочих мест связывает 4 слоя:
1. Общая библиотека скилов и агентов (каждый берёт и пополняет).
2. Данные компании с правами (ассистент видит ровно по роли+уровню).
3. Передача работы между людьми (выход одного = вход другого).
4. Governance (кто что может делать/видеть/утверждать).

## Что уже построено (ЗАЗЕМЛЁННОЕ — это и есть продукт, развивать)
- **Изолированные кабинеты ролей** + переключатель рабочих мест в **правом верхнем углу** (роль / Менеджмент / Владелец). В кабинете роли видны только свои инструменты.
- Кабинет роли = левое меню: **Мой ассистент · Рабочий стол · Передачи · Скилы и агенты · Команда**.
- **Мой ассистент** (экран по умолчанию) — личный чат: попросил → ассистент дал **черновик** → человек **[Принять]/[Вернуть]/[Открыть в рабочей среде]**. Курированные **6 скилов + 3 агента на роль**.
  - **Переключатель сениорити (углублён, слой governance):** правая колонка меняется по уровню. **Стажёр** — панель «короткий поводок» + репутация; «Принять» = **«Отправить наставнику на ревью»** → анимированная цепочка (наставник из `MENTOR` смотрит → принял с 1 правкой → в работу + аудит). **Специалист** — обычный режим. **Руководитель** — **очередь апрувов** (`APPROVALS[role]`): карточки работы команды с риск-бейджами, [Утвердить]/[Вернуть]. **Владелец** — **сводка-исключения** (`EXCEPTIONS[role]`): только то, что вышло за норму/красные линии.
  - **Установка из библиотеки реальна:** кнопки «＋ установить / нанять из библиотеки» ведут в роль-замкнутую библиотеку; «Взять/Нанять» там кладёт скил/агента в ассистента (стор `installStore()`), бейдж **NEW**, кнопка становится «✓ установлено». Счётчики «Мои скилы/агенты» живые.
- **Передачи** (новый экран, слой №3 «химия») — `renderFlow`: поток работы между людьми, **выход одного = вход другого**. 3 сквозных потока (`FLOWS`): «Сделка Гамма» (Продажи→Юр→Финансы→Продажи), «Запуск Lite» (Маркетинг→Дизайн→Юр→Аналитика), «Найм Backend» (HR→Разработка→HR→Разработка). Горизонтальная дорожка с узлами-людьми и стрелками-артефактами; «ваш ход» → кнопка **«Принять и передать дальше → коллега»** (двигает поток, тост, состояние в `window.__FLOWST`). Справа «Моя лента передач»: Ход за вами / Жду коллег / Я передал. Это и есть ответ на «как 10 000 чатов = Среда».
- **Рабочий стол** = своя среда под роль (8 разных): IDE (Разработка), CRM (Продажи), Notebook+SQL (Аналитика), Редактор договора (Юр), ATS (HR), Таблица-модель (Финансы), Канвас (Дизайн), Контент-студия (Маркетинг). Везде **реальные правки руками** (`contenteditable`): печатаешь правку черновика роя → **гейт не пускает дальше, пока не исправил критичное** (тесты краснеют, подпись блокируется, бюджет не утверждается). Списки переключаемы (файлы/сделки/кандидаты/ассеты). Правки **сохраняются** между переключениями.

## Что НЕ продукт (КОСМОС — не развивать, лежит в «Владелец платформы»)
Эти экраны оставлены только как «вижн для инвесторов», вынесены из кабинетов ролей: **Пульс** (организм-граф, `organism.js`), **Контур** (само-улучшение), **Путь** (роадмеп до саморазработки), **Роли**, **Суперсила**, **Battle**, **Лаборатория** (мёртвый код). НЕ тащи их в повседневный продукт. Можно потом релейблить как «Демо / Вижн».

## Как запустить
Открыть `index.html` напрямую в браузере, **или** `npx serve -l 4321 .` (см. `.claude/launch.json`). Без сборки и без интернета (шрифт Inter с CDN — не критично).

## Структура кода
- `index.html` — каркас (топбар + #nav + #stage).
- `assets/mock.js` — все данные. Ключевое: `PERSONAL` (ассистент+скилы+агенты+задачи на роль), `SENIORITY`, `MENTOR` (наставники для стажёра), `APPROVALS` (очередь апрувов руководителя по ролям), `EXCEPTIONS` (сводка-исключения владельца), `FLOWS` (сквозные передачи между людьми), `COCKPITS` (конфиг рабочих сред), `WORKBENCH` (черновики роя с issues по sev), `DEPT_CATALOG`/`LIB_*` (полная библиотека), `DEPARTMENTS`.
- `assets/app.js` — логика. Ключевое: `WORKSPACES` + `renderNav` (изоляция + переключатель), `installStore()`/`installItem()`/`CAT2ROLE` (личный арсенал скилов), `renderAssistant` (личный чат + панели сениорити + установка), `renderFlow`+`flowState()` (Передачи), `ENVS2`+`envCore`+`lineHtml` (рабочие среды и правки руками), `renderLibrary` (опц. `{lock}` под роль → реальная установка), `renderTeam`, `renderStage` (роутер; префиксы `asst:`/`lib:`/`team:`/`flow:`).
- `assets/styles.css` — тёмная тема, аккуратные панели. Акцент emerald `--acc`. Новые блоки в конце: ассистент-сениорити, установка, передачи.
- `assets/organism.js` — КОСМОС (Пульс). Не трогать без причины.

## Сделано в сессии 2026-06-07 (вечер) — связная ткань целиком
✅ Установка скилов/агентов из библиотеки в ассистента · ✅ Экран «Передачи» (3 сквозных потока, передать дальше) · ✅ Цепочка стажёр→наставник (эталон бухгалтер-стажёр на Финансах) · ✅ Очередь апрувов (руководитель) + сводка-исключения (владелец) · ✅ релейбл космоса в nav.
Затем добавлено сверх плана:
- **Передачи ↔ аудит ↔ CEO:** каждая передача/апрув/сабмит пишется в общий `auditLog()` (виден на экране «Аудит» поверх сидов). Дашборд CEO получил живую панель «Передачи в потоке»; в меню Менеджмента — новый экран **«Передачи компании»** (`renderFlowExec`, `flowx`): обзор всех потоков, клик по этапу → провал в кабинет роли.
- **Кнопка «Открыть в рабочей среде»** на активном шаге потока → ведёт в рабочий стол роли.
- **Стажёр → руководитель:** сабмит стажёра падает в `queueStore(id)` и виден в очереди апрувов наставника с бейджем «от стажёра» (стор переживает смену уровня).
- **Баннер входящей работы** в ассистенте: если сейчас ход за этой ролью в каком-то потоке — синяя плашка «Открыть в Передачах».
- **Реализм-проход:** удалён мёртвый космос-код — `renderLab`, `renderDevCockpit`, `renderMarketplace` (~245 строк). Battle обновлён: в «что починили» внесены передачи/сениорити/ассистент/установка.

### Закрыты две концептуальные дыры (по фидбеку «ещё что-то упустили»)
- **Гейт ↔ передача связаны + возврат назад** (`stepGate()`, `flowRet()`): в потоке нельзя «передать дальше», пока в рабочей среде роли открыт критичный риск (sev1) — кнопка заблокирована, ведёт «снять риск в рабочей среде»; снял sev1 в Workbench → передача разблокировалась. Появилась кнопка **«↩ Вернуть автору»** (откат на шаг назад, баннер «вернули на доработку» у автора, запись в аудит с verdict `deny`). Это закрывает «волшебную кнопку» на уровне передач.
- **Данные с правами в кабинете** (слой №2): `ROLE_DATA` в mock + `dataScopeHTML()` — панель «🔐 Данные с правами» в ассистенте показывает, какие витрины видны/закрыты по **роли × уровню**; меняется со сменой сениорити (стажёр 1/6 → владелец 4/6), а **кросс-периметр закрыт даже владельцу** чужого домена.

Всё верифицировано через `preview_eval` (21 экран, консоль чистая). **Скриншоты по-прежнему висят — не использовать.**
Новое в коде: `ROLE_DATA` (mock), `dataScopeHTML`/`SEN_ORDER` (app), `stepGate`/`flowRet` + гейт/возврат в `renderFlow`.

## Ревью скептиков (5 ролевых профи) + план фиксов
Главный диагноз (единогласно): **глубина в один сценарий, нет ширины** — каждый «Рабочий стол» построен вокруг ОДНОГО захардкоженного артефакта; второго объекта нет; чат предлагает 4 задачи, но «Открыть в рабочей среде» всегда ведёт в тот же единственный артефакт. Моки/бэкенд/безопасность из критики исключены по постановке (не задача).

Сделано в ответ на критику:
- ✅ **C — свободный ввод ассистента** маршрутизируется по ключевым словам на ближайшую задачу (`P.tasks`), иначе честное «Уточните…», а не одна заглушка (`renderAssistant.send`).
- ✅ **D — обнаруживаемость:** баннер «Передачи» виден всегда со статусом (ваш ход / ждёте коллегу / передано); счётчик входящих на пункте меню «Передачи» (`ni-badge` в `renderNav`); подсказка у переключателя уровня (`asst-sen-hint`).

Сделано в ответ на критику:
- ✅ **A [ядро] — ширина рабочих объектов.** `WB_QUEUE[roleId]` (по 2 доп. артефакта на роль) + рефактор `envCore`: очередь `[WORKBENCH[id], ...WB_QUEUE[id]]`, состояние правок на элемент (`__ENVST[id]._N`), рейка «Очередь работы» (`data-q`). item0 — родной билдер (релевантен потоку), доп. объекты — дженерик-редактор `genericDraftHtml` (тот же `c.line()` + гейт `majLeft`). `stepGate` читает `_0`. Все 8 столов = очередь из 3, изоляция состояния проверена.
- ✅ **E — оживлены мёртвые элементы.** Сценарии Base/Opt/Stress (finance) живые (множитель на выручку/кэш); витрины (analytics) кликабельны → колонки; слои (design) выбираются (`data-sel`); legal-гейт выровнен до `majLeft` (sev≤2); HR-кнопка «Отправить оффер» → «Пригласить топ-5 на интервью» (этап скрининга).

Добито (последний хвост):
- ✅ **B — чат→объект:** «Открыть в рабочей среде» матчит последнюю задачу по ключевым словам с объектами очереди и открывает нужный (проверено: «квалифицируй лидов» → объект «37 новых лидов»).
- ✅ **Тупик после финала:** очередь помечает выполненные ✓, показывает прогресс «N/M готово» и подсказывает следующий объект (env-act → `sroot.done`).
- ✅ **Навигация при прогоне:** вместо молчаливого игнора — тост «Идёт прогон — дождитесь».
- ✅ **Каналы маркетинга** кликабельны (добавить в кампанию, `data-tg`).
- ✅ **dev:** «Уровни экспертизы» → «🎓 Как уровни ловят ошибки (демо)» (снимает путаницу двух UI).

Осталось:
- ✅ **dev: вкладка `tests.spec.ts` стала редактируемой + гейт на тесте** (закрыт последний хвост). В IDE (Claude Code, `ENVS2.dev`) во вкладке tests виден недостающий кейс «⚠ нет теста на таймаут/отказ 3DS». Появилась панель «➕ Добавьте недостающий тест»: contenteditable-редактор (`[data-test-ed]`), кнопка 💡 «подставить тест» (`DEV_SUGGEST_TEST` — эталонный `it("3DS timeout → graceful decline" … rejects.toThrow("3DS_TIMEOUT")`) и «✓ добавить тест» (`[data-test-add]`). Валидация в `envCore`: текст должен содержать `it(`/`test(` **и** ключевое слово таймаута/отказа, иначе тост-отказ. После добавления — анимация прогона (`st.testRunning`, ~750 мс «⏳ прогон нового теста на таймаут 3DS…»), затем тест рендерится зелёным (`.cc-new` ✓ добавлен вами), покрытие 86%→91%.
  - **Комбинированный гейт Merge** = `codeOk && testAdded && !testRunning`. Терминал имеет 4 состояния: `bad` (есть замечания в коде) · `run` (прогон) · `warn` («143 passed, но нет теста на таймаут — Merge заблокирован») · `ok` («144 passed · покрытие 91%»). PR-чек «✓/⚠ тест на таймаут», шаг плана «Добавил тест на таймаут 3DS», бейдж ⚠ на вкладке tests до добавления. Состояние `st.testAdded`/`st.testText` живёт на item0 (переживает смену вкладок). Всё проверено через `preview_eval`: 4 состояния гейта, валидация (пусто/без таймаута — отклонены), регресс по 8 ролям + очередь dev = 0 ошибок.
- `schema.sql` оставлен read-only осознанно (DDL правится не здесь — это просмотр схемы). Если понадобится — тот же паттерн.

## Реалистичная компания на 150 человек (последние итерации)
- **Штат:** `HEADCOUNT` в mock (dev 52, sales 26, marketing 16, analytics 14, finance 14, design 11, hr 9, legal 8) = `COMPANY_SIZE` 150. Копирайт «8 000» заменён на `COMPANY_SIZE` (3 места).
- **Команды по функциям:** у всех 8 отделов `COCKPITS[id].team` теперь с 5-м полем `fn` (функция). 74 функции суммарно, ~95 человек поимённо. `renderTeam` группирует по `fn`, показывает «N человек · K функций», «ещё M специалистов», и **карточка человека кликабельна** → правая панель «Рой под должность».
- **Рой под должность:** `ROLE_SWARM` (mock, ~70 позиций): `{d:зона ответственности, a:[агенты]}`. `swarmOf(role)` в app. Бейдж человека = размер роя. `DEFAULT_SWARM` — фолбэк.
- **Ассистент = личность + рой должности:** `ASST_WHO` (app) маппит отдел→{имя, должность}; `installStore` сидит агентов из `ROLE_SWARM[who.role]`. Заголовок «Вы: Имя · Должность», блок «🤖 Мой рой» с зоной ответственности; установка из библиотеки дополняет поверх.
- **Оргструктура:** экран `company` (Менеджмент → CEO, `renderCompany`): KPI (150/8/74/150), карточки отделов с долей-баром и функциями, **аккордеон-раскрытие** до функций → людей, «Открыть команду →». Примечание: dev-команда хранится объектами, остальные — массивами; `teamOf` обрабатывает оба формата (важно при любой работе с `COCKPITS[*].team`).
- Полная вертикаль: Оргструктура → Команда (люди по функциям) → Рой под должность → Мой ассистент (тот же рой) → Передачи → Аудит/Governance.

## Редизайн рабочих столов → настоящие профильные интерфейсы (в работе)
Фидбек: «сейчас умный канбан, нужны реальные доступные интерфейсы — чат как наш для большинства, Claude Code для разработки, Nano Banana для дизайнера». Подход: переписываем билдеры `ENVS2[role]`, **сохраняя `envCore`** (очередь объектов, правка руками `c.line/codeLine`, гейт `majLeft`/`s1left`, связь с потоком `stepGate`, аудит). Добавлена общая обработка декоративных кнопок `[data-toast]` в `envCore`.
- ✅ **Разработка = аналог Claude Code** (`ENVS2.dev`): консоль агента — дерево файлов, диалог с агентом + план-чеклист (отмечается по мере правок), редактируемый diff, живой терминал, PR-сайдбар с гейтом Merge. CSS `.cc*`.
- ✅ **Дизайн = аналог Nano Banana** (`ENVS2.design`): промпт + пресеты + «Сгенерировать», 4 варианта (переключаются, меняют визуал), сгенерированное изображение-мок, правки a11y промптом (💡) или руками, гейт «Принять в дизайн-систему». CSS `.nbb*`.
- ✅ **Чат для большинства = аналог нашего чата** (`ENVS2.sales/marketing/hr` → `chatEnvBuild`): разговор слева (пузыри, чипы из `PERSONAL.tasks`, composer) + артефакт-канвас справа (`c.WB.draft` правится руками + гейт + приём с ролевой меткой `ACCEPT_LABEL`). Чат хранится в `c.st.chat` (per-item). CSS `.chatx*`. Обвязка `[data-ask]`/composer/`[data-toast]` добавлена в `envCore`.
- ✅ **Спец-инструменты доведены до flagship-уровня** (одобрено: «так и делаем их проф-инструментами»):
  - **finance** = FP&A-модель: AI-строка допущений (`fin-ai`, пресеты), расширенная таблица (выручка/расходы/валовая/маржа/НДС/чистый кэш), график кэша по месяцам, живые сценарии Base/Opt/Stress с сезонностью.
  - **analytics** = notebook: AI-строка «спроси данные словами» → SQL, кнопка ▶ Запустить, результат с метаданными (3 строки · 0.4 c) + воронка-график с подписями, кликабельные витрины+колонки.
  - **legal** = контракт-ревью: AI-проверка, пункты с риск-точками по sev, риск-резюме (скор + счётчики sev1/sev2), E&O-дисклеймер, гейт подписи `majLeft`.
  - Общие классы AI-строки `.fin-ai*` переиспользуются; обвязка `[data-toast]` в `envCore`.

### Итог: у каждой роли — свой настоящий интерфейс
dev → **Claude Code** · design → **Nano Banana** · sales/marketing/hr → **чат с артефактом** · finance → **модель** · legal → **редактор договора** · analytics → **notebook**. Все на `envCore` (очередь объектов, правка руками, гейт, связь с потоком/аудитом). Если захотят — finance/legal/analytics тоже легко перевести на чат (одна строка: `(root,d)=>envCore(root,d,c=>chatEnvBuild(d,c))`).

## «Живой» ИИ — стриминг/движение (улучшение №1 по моему анализу)
Главная слабость была: всё статично (готовые черновики), читалось как «поделка». Добавлено движение без бэкенда:
- `typeInto(node,text,scroller,done)` — печать по буквам с паузой «на размышление» (360 мс) + мигающая каретка (`.streaming::after`).
- `streamChat(root,st,u,a)` — поток в чат-тред: юзер → агент печатает → коммит в `st.chat`. Подключено в `envCore` для `[data-ask]` и composer (sales/marketing/hr).
- **Мой ассистент** (`renderAssistant.ask`): ответ печатается (`am-typing`/`am-stream`) → затем готовый черновик с кнопками.
- **Nano Banana**: кнопка `.nbb-gen` → оверлей «✨ генерирую варианты…» (shimmer 950 мс) → результат.
- **Notebook**: `.nb-run` → «⏳ выполняется…» 700 мс → тост.
- **Claude Code**: мигающая каретка в командной строке (`.cc-m.user::after`).
Примечание: в фоновом превью таймеры троттлятся (печать медленная) — в реальном браузере 14 мс/символ, быстро. Логика завершения через `done`-callback.

✅ **Демо-тур (presenter mode)** — `TOUR` (9 шагов) + `tourGo/drawTourCap/tourSpot/endTour`, лаунчер `#tourFab` (injectTour в init). Ведёт по сделке «Гамма» насквозь: компания → личный чат → артефакт+контроль → передача → юрист (риск-гейт) → финансы → аудит → взгляд CEO → «почему это Среда». Каждый шаг: navTo нужного экрана + карточка-подпись (Назад/Далее/Закрыть, Esc) + подсветка целевого блока (`.tour-spot`). CSS `#tourFab/#tourCap/.tc-*`.

✅ **Единый traceable объект «Гамма»** — реальные связи, один источник истины `flowState().gamma`:
- `dealTraceHTML()` — «След сделки»: 4 этапа (Оля/Продажи → Ира/Юр → Аня/Финансы → Оля/закрытие) со статусом done/active/wait, кликабельны → `navTo('flow:'+role)`.
- **Кросс-связь работой:** приём КП в столе продаж (env-act, sales item0, gate пройден) двигает `flowState().gamma` 0→1 + запись в аудит → тот же объект сдвигается в Передачах, Аудите и на дашборде CEO. Не нарратив, а одни данные.
- Панель «🔗 След сделки «Гамма»» на дашборде CEO (`#dealTrace`, wired `navTo`). Старт `gamma:0` (сделка на этапе продаж), чтобы связь была наглядной. CSS `.dt*`.
- Проверено: продажи приняли КП → Оля✓, активна Ира; аудит-топ «КП «Гамма» принято → передано юристу»; клик по этапу проваливает в Передачи.

Все 3 улучшения из анализа сделаны: живой ИИ · демо-тур · traceable «Гамма».

## «Пульс» оживлён — поток задач в фоне (по просьбе «видеть, как бегут сотни задач»)
- `organism.js`: фоновый `ambient()` стал плотным и масштабируемым (`ambientBurst()`: Стартап ×2 / Скейлап ×4 / Корпорация ×7, тик 420 мс); задачи текут ядро→отдел→модель→обратно, узлы пульсируют, связи светятся.
- **Именованные задачи на частицах**: `spawn(...,label)` + `TASK_LABELS` (PR #483, КП, тест, 38-ФЗ, счёт…) — ~35% фоновых частиц подписаны, метка рисуется на лету (fade по t).
- **Всплеск** `Organism.surge(n)` — волна именованных задач из ядра; вызывается из «Запустить рой» (`Organism.surge(40)`).
- **Живой счётчик потока** (app.js `renderOrganism`, `window.__pulseTimer`, чистится в `renderStage`): «задач в работе сейчас» (Стартап ≈10, Корпорация ≈22 000+ = `model.agents*0.18`), «выполнено сегодня» (тикает), лента «что происходит сейчас» (5 строк, обновление 900 мс). CSS `.org-flow/.of-*`.
- Проверено в реальном Chrome пользователя (Claude-in-Chrome MCP): 54/54 экрана рендерятся, 0 JS-ошибок, 0 ошибок консоли, стриминг ИИ доходит до конца, поток масштабируется, surge без ошибок.

## Последние добавления (эта сессия, после «Пульс оживлён»)
- **Задачи в «Пульсе» покрашены по типу** (`organism.js` `TASKS_T`): код 🟦 · продажи 🟩 · деньги 🟧 · юр 🟥 · дизайн 🟪 · данные (cyan) · HR 🩷. Метки на частицах в цвет домена. `C.pink` добавлен. Легенда цветов в панели потока (`.of-legend`).
- **Пул моделей расширен** (`MODELS` в mock + `NAMED_MODELS`/`modelColor` в organism): +GPT o3, Gemini 2.5 Pro, DeepSeek V3, Mistral Large, Qwen 2.5, Grok 2, Command R+ (13 именованных в кольце), поле modeldots 48→96. 9 вендоров.
- **Пульс отдела** (`renderDeptPulse` в app.js, пункт меню `dpulse:<role>` 🫀 в кабинете роли): заземлённый хаб-граф — ядро отдела + кольцо его функций (из `COCKPITS[role].team` по `fn`), цветные задачи-пилюли летят по спицам от ядра к функциям, живые счётчики «в работе/выполнено» (масштаб отдела ≈ hc×0.8), лента по людям. Данные `DEPT_TASK`/`DP_CROSS`. CSS `.dp-*`.
  - ⚠️ был баг: ядро не центрировалось (не применялся `style.left/top`) — ИСПРАВЛЕНО (layout центрирует ядро, рисует SVG-спицы `#dpLinks`, эллипс со смещением радиуса через одну → без пересечений 17 узлов). Если правишь layout — проверяй центрирование и overlap на широком экране.
- **Инфра:** превью-сервер `npx serve -l 4321` за сессию падал несколько раз (между проверками) — если страница показывает голый «localhost», перезапусти preview (`preview_start name=sreda`) или `npx serve`.
- Проверка велась в реальном Chrome пользователя (Claude-in-Chrome MCP, tabId 1030938600): 54 экрана рендерились, 0 ошибок; печать/стриминг доходят до конца (в фоновом превью таймеры троттлятся — не пугаться).

## Новый чат / новый терминал (фидбек: «а как открыть новый чат или терминал?»)
Была дыра: «Мой ассистент» — один статичный тред, IDE — один статичный терминал, «новый» открыть негде (для «как Claude Code / наш чат» — стыдно). Закрыто:
- **Мульти-чат в «Мой ассистент»** (`asstChats(id,hi)` → `window.__ASSTCHATS[id]={list:[{title,msgs}],active}`). Над тредом — строка вкладок (`.asst-tabs`): чаты + «＋ Новый чат». Новый чат = свежий тред с приветствием `P.hi`; авто-название по первому сообщению (`ch._named`); вкладки закрываются (×, кроме последней); история каждого чата живёт отдельно и переживает перерисовку/смену уровня. `msgs` в `draw()` переуказывается на `chats.list[active].msgs`.
- **Терминал с вкладками в IDE** (`ENVS2.dev`): вкладка «tests» (живая строка гейта) + «＋ терминал» (новый shell `zsh N`). Промпт `sreda ❯` с инпутом — команды интерпретирует `devTermRun(cmd,g)` и **отражают реальный гейт**: `run tests`/`build`/`coverage` краснеют/желтеют/зеленеют в зависимости от `codeOk`+`testAdded`; `git status` показывает изменённые файлы (tests.spec.ts — только если тест добавлен); `ls`, `help`, `clear`, иначе `command not found`. Лог каждого терминала per-item (`st.terms`/`st.termActive`), переживает смену файловых вкладок. Обвязка инпута/＋/выбора — в `envCore` (как и весь dev-UI).
- Проверено `preview_eval`: 8 ассистентов с вкладками + новый чат (история сохраняется), терминал по всем командам в красном/жёлтом/зелёном состоянии, новый терминал, clear/help, 0 ошибок консоли, регресс по 8 средам чист.

## Правила для агента (важно)
- Заземлённо и контролируемо. Каждое действие ИИ = **черновик, который человек правит**, не «волшебная кнопка».
- НЕ возвращай космос в кабинеты ролей.
- Проверяй через `preview_eval` (DOM/состояние). **Скриншоты превью в прошлой сессии висли** — не полагайся на них, верифицируй через eval.
- Стек прежний (vanilla JS + моки). Бэкенд не вводить без явной просьбы.
- Пользователь хочет глубину и не любит «поделки» — прорабатывай до конца, не сворачивайся рано.
